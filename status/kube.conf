# vim:set ft=tmux:
%hidden MODULE_NAME="kube"

# Requires https://github.com/jonmosco/kube-tmux

set -ogq "@onedark_${MODULE_NAME}_icon" "󱃾 "
set -ogqF "@onedark_${MODULE_NAME}_color" "#{@thm_blue}"
set -ogqF "@onedark_kube_context_color" "#{@thm_red}"
set -ogqF "@onedark_kube_namespace_color" "#{@thm_sky}"
set -ogq "@onedark_${MODULE_NAME}_text" \
   "#(${TMUX_PLUGIN_MANAGER_PATH}kube-tmux/kube.tmux 250 #{@onedark_kube_context_color} #{@onedark_kube_namespace_color})"

source -F "#{d:current_file}/../utils/status_module.conf"
